# 🏗️ ICCT26 Backend - Fixed Architecture

## System Architecture After Fixes

```
┌─────────────────────────────────────────────────────────────────┐
│                     FastAPI Application                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           NEW ADMIN ENDPOINTS (SYNC)                    │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  GET /admin/teams                  (Session → TeamReg)  │  │
│  │  GET /admin/teams/{team_id}        (Session → TeamReg)  │  │
│  │  GET /admin/players/{player_id}    (Session → Player)   │  │
│  │  GET /status                       (Database health)    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           ↓                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │      OLD ENDPOINTS (ASYNC - Still Working)              │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  POST /register/team               (AsyncSession)        │  │
│  │  GET /teams                        (AsyncSession)        │  │
│  │  GET /send-test-email              (SMTP)               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           ↓                                     │
└─────────────────────────────────────────────────────────────────┘
         │                    │                    │
         ↓                    ↓                    ↓
    ┌─────────┐        ┌─────────┐        ┌────────────────┐
    │  SYNC   │        │  ASYNC  │        │  EMAIL (SMTP)  │
    │   DB    │        │   DB    │        │                │
    │Session  │        │AsyncSes │        │  Gmail/SendGrid│
    └────┬────┘        └────┬────┘        └────────────────┘
         │                  │
         └──────┬───────────┘
                ↓
        ┌──────────────────┐
        │ PostgreSQL DB    │
        │ (Render Cloud)   │
        ├──────────────────┤
        │ Tables:          │
        │ • team_reg.      │
        │ • captains       │
        │ • vice_captains  │
        │ • players        │
        │ • teams (new)    │
        └──────────────────┘
```

---

## Request Flow - Admin Endpoints

```
┌─ CLIENT REQUEST ──────────────────────────────────────────┐
│ GET /admin/teams                                          │
└─────────────────────────┬────────────────────────────────┘
                          ↓
        ┌─ FASTAPI ROUTING ────────────┐
        │ @app.get("/admin/teams")     │
        └──────────────┬────────────────┘
                       ↓
        ┌─ DEPENDENCY INJECTION ───────┐
        │ Depends(get_db) →            │
        │ SessionLocal()               │
        └──────────────┬────────────────┘
                       ↓
        ┌─ SYNCHRONOUS DATABASE QUERY ┐
        │ db.query(TeamRegistrationDB) │
        │    .all()                    │
        └──────────────┬────────────────┘
                       ↓
        ┌─ DATABASE CONNECTION ────────┐
        │ PostgreSQL (Sync psycopg2)   │
        └──────────────┬────────────────┘
                       ↓
        ┌─ DATABASE OPERATIONS ────────┐
        │ SELECT * FROM team_regist... │
        │ SELECT * FROM captains       │
        │ SELECT * FROM vice_captains  │
        │ COUNT players                │
        └──────────────┬────────────────┘
                       ↓
        ┌─ PYTHON OBJECT MAPPING ──────┐
        │ TeamRegistrationDB Objects   │
        │ CaptainDB Objects            │
        │ ViceCaptainDB Objects        │
        │ PlayerDB Objects (count)     │
        └──────────────┬────────────────┘
                       ↓
        ┌─ RESPONSE FORMATTING ────────┐
        │ Convert to dictionaries      │
        │ Include team info            │
        │ Include captain info         │
        │ Include player count         │
        └──────────────┬────────────────┘
                       ↓
        ┌─ JSON RESPONSE ──────────────┐
        │ {                            │
        │   "success": true,           │
        │   "teams": [...]             │
        │ }                            │
        └──────────────┬────────────────┘
                       ↓
┌─ CLIENT RECEIVES ─────────────────────────────────────────┐
│ HTTP 200 OK                                               │
│ {"success": true, "teams": [...]}                         │
└───────────────────────────────────────────────────────────┘
```

---

## Database Dependency Injection

```
┌──────────────────────────────────────────┐
│  FastAPI Application Startup             │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Load Environment Variables from .env    │
│  DATABASE_URL = "postgresql://..."       │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  database.py                             │
│  ├─ Convert asyncpg → psycopg2          │
│  ├─ Create Engine (sync)                │
│  ├─ Create SessionLocal                 │
│  └─ Define get_db() dependency          │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Endpoint Request                        │
│  @app.get("/admin/teams")                │
│  def admin_get_teams(                    │
│      db: Session = Depends(get_db)       │  ← Dependency!
│  )                                       │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  get_db() Called                         │
│  ├─ Creates SessionLocal()               │
│  ├─ Yields session to endpoint           │
│  └─ Closes after response                │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Endpoint Executes                       │
│  db.query(...) uses injected session     │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Database Operations Complete            │
│  Session Automatically Closed            │
└──────────────────────────────────────────┘
```

---

## Data Flow - Team Registration to Admin View

```
REGISTRATION (POST /register/team)
    ↓
Create: TeamRegistrationDB
Create: CaptainDB
Create: ViceCaptainDB  
Create: PlayerDB (multiple)
    ↓
Store in PostgreSQL
    ↓
ADMIN VIEW (GET /admin/teams)
    ↓
Query: TeamRegistrationDB
Join:  CaptainDB
Join:  ViceCaptainDB
Count: PlayerDB
    ↓
Format Response:
{
  "teamId": "...",
  "teamName": "...",
  "captainName": "...",
  "playerCount": 12
}
    ↓
Return to Client (200 OK)
```

---

## Error Handling Flow

```
┌──────────────────────────────────────────┐
│  API Request                             │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Try Block                               │
│  ├─ Get database session                │
│  ├─ Execute query                       │
│  └─ Format response                     │
└──────────┬───────────────────────────────┘
           ↓ (Error occurs)
┌──────────────────────────────────────────┐
│  Exception Handling                      │
│                                          │
│  HTTPException (Controlled)              │
│  ├─ 404 Not Found                       │
│  ├─ 422 Validation Error                │
│  ├─ 500 Server Error                    │
│  └─ Return JSON error response           │
│                                          │
│  Generic Exception (Unhandled)           │
│  ├─ Log to console                      │
│  ├─ Catch in except block               │
│  └─ Raise HTTPException(500)            │
└──────────┬───────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Error Response to Client                │
│  {                                       │
│    "detail": "Team not found"            │
│  }                                       │
│  HTTP 404/500                            │
└──────────────────────────────────────────┘
```

---

## Configuration Files

```
.env (or .env.local)
├─ DATABASE_URL=postgresql+asyncpg://icctadmin:...
│  (automatically converted to sync)
├─ SMTP_SERVER=smtp.gmail.com
├─ SMTP_PORT=587
├─ SMTP_USERNAME=...
└─ SMTP_PASSWORD=...

main.py
├─ Load .env variables
├─ Create async engine (old endpoints)
├─ Create sync engine (admin endpoints)
├─ Define routes
└─ Define dependencies

database.py
├─ Sync database configuration
├─ SessionLocal factory
├─ get_db() dependency
└─ URL conversion logic

models.py
├─ Team model (for future)
└─ Player model (for future)

Old async models in main.py
├─ TeamRegistrationDB (for existing data)
├─ CaptainDB
├─ ViceCaptainDB
└─ PlayerDB
```

---

## Key Architectural Improvements

| Before | After | Impact |
|--------|-------|--------|
| Mixed async/sync | Separated configs | ✅ No conflicts |
| One Base instance | Multiple Bases | ✅ No table redefinition |
| Async everywhere | Sync for admin | ✅ Simpler operations |
| Wrong types (int) | Correct types (str) | ✅ Proper validation |
| Generic errors | Specific HTTP codes | ✅ Better debugging |
| Async models | Old + new models | ✅ Backwards compatible |

---

## Production Deployment Architecture

```
┌────────────────────────────────────────────────┐
│         GitHub Repository                      │
│  ├─ main.py (Fixed admin endpoints)           │
│  ├─ database.py (PostgreSQL sync)             │
│  ├─ models.py (SQLAlchemy models)             │
│  └─ requirements.txt (All dependencies)       │
└────────────────┬─────────────────────────────┘
                 ↓ Push to main
        ┌────────────────────────┐
        │  Render CI/CD Pipeline │
        └────────┬───────────────┘
                 ↓ Deploy Latest
        ┌────────────────────────┐
        │  Render Web Service    │
        │  ├─ Python 3.13        │
        │  ├─ Uvicorn server     │
        │  └─ Port 8000 → 443    │
        └────────┬───────────────┘
                 ↓
        ┌────────────────────────┐
        │ Render PostgreSQL      │
        │ ├─ Cloud database      │
        │ ├─ Automatic backups   │
        │ └─ High availability   │
        └────────────────────────┘
                 ↓
        ┌────────────────────────┐
        │ Frontend/Client        │
        │ Calls:                 │
        │ /admin/teams           │
        │ /admin/teams/{id}      │
        │ /admin/players/{id}    │
        └────────────────────────┘
```

---

**All systems ready for production! 🚀**

Last Updated: November 8, 2025
